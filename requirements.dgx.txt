# === DGX Spark (ARM64 + Blackwell) 專用依賴 ===
# 注意：PyTorch 已在 NGC 容器中預裝，無需再安裝

# === Web Framework ===
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# === HuggingFace（固定版本避免衝突）===
huggingface_hub>=0.20.0,<0.25.0
# transformers 必須固定版本，NGC 容器預裝的版本太新
transformers>=4.36.0,<4.40.0

# === SpeechBrain（pyannote Pipeline 需要）===
speechbrain>=1.0.0

# === WhisperX + Diarization ===
whisperx>=3.1.5

# === pyannote 額外依賴 ===
pyannote.audio>=3.1.0
asteroid-filterbanks>=0.4.0

# === Task Queue ===
celery[redis]>=5.3.0
redis>=5.0.0

# === HTTP Client ===
httpx>=0.27.0

# === Configuration ===
pydantic>=2.5.0
pydantic-settings>=2.1.0

# === Utilities ===
numpy>=1.24.0
python-json-logger>=2.0.0

# === 簡繁轉換 ===
opencc-python-reimplemented>=0.1.7
